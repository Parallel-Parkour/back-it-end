// 'use strict';

// const { createUser, management, email, username, password } = require('./createUser.js');
// const { login, user, pass } = require('./loginUser.js');

// // user is created and stored in auth0
// createUser(email, username, password).then(() => login(user, pass)).catch((e) => { console.log('Login error: ', e); });

// // login(user, pass);
// console.log('Login successful: ', user);